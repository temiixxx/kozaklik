# 🚀 Быстрый старт: Сборка и публикация APK

## Шаг 1: Соберите APK

### Через командную строку (самый простой способ):

```bash
# Windows
.\gradlew.bat assembleDebug

# APK будет создан в:
# app\build\outputs\apk\debug\app-debug.apk
```

### Через Android Studio:

1. Откройте проект
2. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
3. Дождитесь завершения

## Шаг 2: Создайте Release на GitHub

1. **Перейдите на GitHub**: https://github.com/temiixxx/kozaklik
2. **Нажмите "Releases"** (в правом меню)
3. **Нажмите "Create a new release"**
4. **Заполните:**
   - **Tag version**: `v1.0.0` (создайте новый тег)
   - **Release title**: `Версия 1.0.0 - Кликер козы`
   - **Description**: Скопируйте из `build-and-release.md` или напишите свой
5. **В разделе "Attach binaries"**:
   - Перетащите файл `app-debug.apk` из `app\build\outputs\apk\debug\`
   - Или нажмите "selecting them" и выберите файл
6. **Нажмите "Publish release"**

## Готово! 🎉

Теперь любой может скачать APK со страницы Releases!

## Прямая ссылка на скачивание

После создания релиза, ссылка будет:
```
https://github.com/temiixxx/kozaklik/releases/latest/download/app-debug.apk
```

(Если файл называется `app-debug.apk`)

## Для будущих версий

1. Обновите `versionCode` и `versionName` в `app/build.gradle.kts`
2. Соберите новый APK
3. Создайте новый релиз с новым тегом (например, `v1.1.0`)
