# 🚀 Инструкция: Сборка APK и размещение на GitHub

## Шаг 1: Соберите APK

### Вариант A: Через Android Studio
1. Откройте проект в Android Studio
2. **Build** → **Build Bundle(s) / APK(s)** → **Build APK(s)**
3. Дождитесь завершения сборки
4. APK будет в: `app/build/outputs/apk/debug/app-debug.apk`

### Вариант B: Через командную строку

```bash
# Windows (PowerShell или CMD)
.\gradlew.bat assembleDebug

# После сборки APK будет в:
# app\build\outputs\apk\debug\app-debug.apk
```

## Шаг 2: Подготовка к загрузке на GitHub

1. Скопируйте собранный APK в папку `releases/`:
   ```bash
   # Переименуйте для понятности
   copy app\build\outputs\apk\debug\app-debug.apk releases\clicker-kozy-v1.0.0.apk
   ```

2. Или просто используйте APK из `app/build/outputs/apk/debug/`

## Шаг 3: Создайте Release на GitHub

### Через веб-интерфейс:

1. Перейдите на https://github.com/temiixxx/kozaklik
2. Нажмите **"Releases"** (справа в меню)
3. Нажмите **"Create a new release"** или **"Draft a new release"**
4. Заполните форму:
   - **Choose a tag**: Создайте новый тег `v1.0.0`
   - **Release title**: `Версия 1.0.0 - Кликер козы`
   - **Description** (можно использовать этот текст):

```markdown
## 🎉 Первый релиз Кликер козы!

### ✨ Новые функции:
- Система престижа с UI
- Квесты с прогресс-барами
- Временные бусты
- События (Двойной день)
- Форматирование чисел (1K, 1M, 1B)
- Экспоненциальный рост цен

### 📱 Установка:
1. Скачайте APK файл ниже
2. На Android: Настройки → Безопасность → Разрешить установку из неизвестных источников
3. Установите APK

### 🐛 Известные проблемы:
- Нет известных критических ошибок

### 📝 Примечания:
- Минимальная версия Android: 10 (API 29)
- Размер APK: ~15-20 МБ
```

5. В разделе **"Attach binaries"** перетащите APK файл или нажмите **"selecting them"**
6. Выберите файл `app-debug.apk` или `clicker-kozy-v1.0.0.apk`
7. Нажмите **"Publish release"**

## Шаг 4: Обновите README (опционально)

После создания релиза GitHub автоматически создаст ссылку на скачивание. Вы можете обновить README, добавив прямую ссылку:

```markdown
## 📥 Скачать

[![Download APK](https://img.shields.io/badge/Download-APK-brightgreen)](https://github.com/temiixxx/kozaklik/releases/latest/download/app-debug.apk)

Или перейдите на [страницу релизов](https://github.com/temiixxx/kozaklik/releases)
```

## 🔄 Для будущих релизов

1. Обновите `versionCode` и `versionName` в `app/build.gradle.kts`
2. Соберите новый APK
3. Создайте новый релиз с новым тегом (например, `v1.1.0`)
4. Загрузите новый APK

## 📦 Размер APK

Ожидаемый размер APK: **15-25 МБ** (debug версия может быть больше)

Для уменьшения размера можно:
- Включить ProGuard/R8 (`isMinifyEnabled = true`)
- Использовать App Bundle вместо APK
- Оптимизировать ресурсы
